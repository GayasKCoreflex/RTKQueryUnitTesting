import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    flex: 1,
    borderWidth: 1,
    padding: 10,
    borderColor: '#ccc',
    borderRadius: 5,
  },
});

export default styles;
